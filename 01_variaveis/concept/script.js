var x = 6
let y = 7
const z = 8

//      Váriaveis são containers para armazernar dados (armazenando o nome de dados e os valores de dados)


var nome = "Danilo" //A palavra-chave 'var' é usada em todo o código JavaScript desde 1995, já as palavras-chave 'let' e 'const' foram adicionadas ao JavaScript em 2015;

console.log(nome);
console.log(`O meu nome é ${nome}`);

let um = 1,
    dois = 2,
    tres = 3;

console.log(tres * (um + dois + tres));

//Variáveis são a forma que a gente tem de salvar valores em um espaço da memória do COMP...

//Quando o valor da variável é salvo, ela se torna um STATEMENT.

//Com os statement's podemos realizar expressões matemáticas e operações lógicas.