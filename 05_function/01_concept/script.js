const c = 5;
const d = 6;

// 'function'.

// Uma 'function' em JavaScript é um código de blocos projetado para executar uma tarefe expecífica. Uma 'function' é executada quando é chamada de algum modo expecífico.

// Uma 'function' é definida por uma palavra-chave chamada "function", seguida de um nome atribuído a esta função, por sua vez seguido por parênteses;

//                  parâmetros separados por vírgula;
function multiplicar(x = 0, y = 0) {
  return x * y; // '{codeBlock}' bloco de códigos a ser executado.
}

/*
        Sintaxe:
            function nomeDaFunção(parâmetro1, Parâmetro2, etc) {
                código a ser executado
            }
*/
